# go development

- target Go 1.27.1 for the root module and development toolchain;
- keep command entry points in `cmd/` and application code in `internal/`;
- keep the archive and metadata human-readable and independent of provider types;
- store each thought's authored content in `post.md`; use `---` lines outside fenced code to separate thread posts;
- use the standard library before adding a dependency;
- update `AGENTS.md` when repository conventions change.
